import { Fragment } from 'react';
import classes from './FilterBar.module.scss';
const FilterBar = (props) => {
    return <Fragment>
        <div className={classes.filterBar}>filterBar</div>
    </Fragment>
}
export default FilterBar;