import styles from '../styles/components/Mobile.module.scss';

export default function Mobile(props: any) {
    // return <div className={styles.panel}>{props.children}</div>;
    return <h1>welcome to mobile</h1>;
}
