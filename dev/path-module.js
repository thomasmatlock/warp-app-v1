var path = require('path');
var filename = path.basename('/Users/Refsnes/demo_path.js')); // basename is just the file in questions actual basename
var directories = path.dirname('/Users/Refsnes/demo_path.js'); // dirname is opposite of basename: it returns the directory name of a file, rather than the file name itself
console.log(os.userInfo().homedir);