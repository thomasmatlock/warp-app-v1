/* eslint-disable no-unused-expressions */
/* eslint-disable no-undef */
//www.electronjs.org/docs/api/remote#methods

remote.require(module);
remote.getCurrentWindow();
remote.getCurrentWebContents();
remote.getGlobal(name);
remote.process;
